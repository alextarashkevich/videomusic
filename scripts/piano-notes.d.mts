// The build scripts are plain Node ESM so they can run before anything is compiled. This
// is here purely so the test comparing that list against the engine's typechecks.
export const PIANO_NOTES: string[]
